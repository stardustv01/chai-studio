# Chai Studio — faithful correction V2

This folder corrects the rejected font-derived V1. It reconstructs the approved
`03-balanced-hybrid.png` artwork as custom SVG geometry instead of substituting
a standard Devanagari font.

Nothing in this folder has been moved into or wired into the live application.
V1 remains untouched as a record of the rejected direction.

## Review first

1. `proof/reference-compare.png` — approved raster, corrected vector, compact
   symbol, UI scale test, and a registration overlay.
2. `snapshots/contact-sheet.jpg` — exact animation states at 0, 0.18, 0.36,
   0.62, 2.39, and 2.40 seconds.
3. `proof/cyan-playhead-keyframes.png` — focused playhead landing proof.

## What changed from V1

- Restored the approved wide, custom `चाय` silhouette.
- Restored the long amber gesture as part of the `च`, not a tiny underline.
- Restored the compact `चा` construction used by the approved app symbol.
- Rebuilt the headline as one uninterrupted editorial timeline with three
  deliberate ticks.
- Preserved the reference's English companion outlines instead of replacing
  them with another font.
- Kept the cyan playhead in the approved optical position.

## Vector masters

- `masters/chai-lockup-color.svg`
- `masters/chai-lockup-mono.svg`
- `masters/chai-wordmark-color.svg`
- `masters/chai-symbol-color.svg`
- `masters/chai-symbol-mono.svg`
- `masters/chai-app-icon.svg`

All masters are self-contained SVG paths. The PNG reference is not embedded in
any shipping candidate.

## Motion behavior

- 0–220 ms: continuous timeline reveals left to right.
- 180–360 ms: compact `चा`, amber gesture, and cyan playhead arrive.
- 360–560 ms: the `य` body completes the full signature.
- 460–620 ms: `Chai Studio` companion settles into the lockup.
- 620–2400 ms: final state holds for inspection; no loop.
- Reduced motion: completed final state appears immediately.

### Creative A/B candidate — Precision Awakens Warmth

The original clean reveal remains untouched. A separate review candidate lives
at `compositions/creative-reveal.html` and is generated by
`scripts/build-creative-composition.mjs`.

- 120–540 ms: twin cyan signals draw the ivory editorial timeline inward from
  both ends and converge at the approved playhead axis between the letterforms.
- 560–980 ms: the custom Hindi geometry completes before the cyan playhead
  enters, preserving the letterforms as the main reveal.
- 580–1370 ms: a restrained amber bloom releases the warm gesture, then clears
  completely so the final lockup stays faithful.
- 1000–1320 ms: the cyan playhead grows inward from its top and bottom tips;
  both halves merge between the Hindi letterforms.
- 1420–1720 ms: the English companion settles without bounce or spectacle.
- 1720–2400 ms: the exact approved final geometry holds.

The creative candidate uses one causal idea—precision awakens warmth—not a
collection of unrelated effects. It remains seek-safe, finite, and loop-free.

The implementation uses separated SVG layers and one paused, seek-safe GSAP
timeline. Three.js is intentionally excluded because it adds no fidelity to
this flat identity.

## Verification

HyperFrames 0.7.63 passed with zero lint, runtime, layout, or motion findings.
The exact 2.40-second boundary retains the approved final state.

## Honest limitation

The approved source is a generated raster, not an original vector file. V2 is
a cleaned, high-fidelity reconstruction of that visible artwork. It should
still receive visual approval—and ideally a fluent Devanagari reader's signoff—
before being treated as the permanent brand master.

No video render or live-project integration has been performed.
